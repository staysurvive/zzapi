Use case: ui-mockup
Asset type: zzapi homepage below-fold visual direction
Primary request: Design a new production-quality, scrollable homepage continuation for zzapi. The output starts immediately after the existing frozen Hero and redesigns only the content below it. The concept is a "Protocol Atlas": wide cinematic information bands reveal model identity, the gateway's routing field, current catalog topology and a compact developer protocol surface.
Input images:
- Image 1: exact current zzapi Hero and brand-language reference. Do not redraw or redesign the Hero; the generated canvas begins below it.
- Image 2: hierarchy reference for a large model identity object only. Do not copy its purple palette, certification claims, circular percentage, layout, typography, logo, copy, or components.
- Image 3: rhythm reference for a large data object only. Do not copy its purple/orange palette, chart, accordion, layout, typography, logo, copy, or components.

Target dimensions: a 1440px-wide scrollable landing page direction, rendered on a 2160x3840 portrait canvas. Show only the below-fold continuation; no browser chrome, no device frame, no duplicated navigation, no Hero reconstruction.

Audience and goal: developers and technical buyers evaluating an AI API gateway. Make the whole product feel like one infrastructure atlas instead of a dashboard or feature inventory.

Visual system: preserve zzapi's current minimal premium technical brand from Image 1. White and near-white bands, dense black headings, graphite metadata, pale silver coordinate lines, very restrained zzapi blue route signals. Use large negative space, disciplined 1280px content width, precise alignment, premium print-like pacing. No purple, no dark-blue takeover, no gradient backgrounds, no glassmorphism, no neon, no floating decorative cards.

Information hierarchy and layout:
- Begin with a large, sparse title band that creates a seamless transition from the frozen Hero.
- First product object is a full-width "identity plate": oversized model name on the left, a horizontal taxonomy of Configured / Route available / Observed / Not reported on the right, and a thin baseline that aligns every field. This is one coherent plate, not a dashboard panel.
- Next, use a panoramic routing field across nearly the full width. The zzapi Gateway is a restrained central anchor; one request trajectory enters, then splits by policy into current catalog models and rejoins as a response. Add only a few factual labels, making the geometry itself explain the system.
- Follow with a broad infrastructure band showing protocol compatibility as parallel endpoint lanes, not cards or badges. Use one subtle pale-gray surface change to separate the band.
- Developer section uses a large typographic statement plus a compact, edge-aligned code specimen and a model selection row that resembles a technical index.
- End with a minimal CTA band with one primary and one secondary action, then existing project attribution/footer language.

Text (verbatim):
- "The model catalog is a living protocol map"
- "MODEL IDENTITY"
- "deepseek-v4-flash"
- "Configured"
- "Route available"
- "Observed"
- "Not reported"
- "REQUEST ROUTE"
- "Client request"
- "zzapi Gateway"
- "Policy-based routing"
- "Current catalog"
- "Response"
- "OpenAI-compatible"
- "POST /v1/chat/completions"
- "One endpoint, switch across the current catalog"
- "Open Console"
- "Read Docs"

Constraints: render only truthful qualitative states; no official certification; no Verified; no Authentic Model; no 100%; no 99.99%; no latency, uptime, request-count, cost, or unsupported capability claims. Do not create a bento grid, feature cards, radial percentage, dashboard chart, or dense node mesh. Render the listed text directly, exactly once where used, with no invented marketing numbers, no third-party logos, no watermark.

Responsive intent: each horizontal band must have a clear mobile counterpart: stacked identity rows, a linear route, endpoint lanes with horizontal scrolling confined to their own code surface, and reachable CTA actions.
