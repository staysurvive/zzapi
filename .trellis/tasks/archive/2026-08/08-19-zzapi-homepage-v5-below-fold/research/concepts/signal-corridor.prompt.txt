Use case: ui-mockup
Asset type: zzapi homepage below-fold visual direction
Primary request: Design a new production-quality, scrollable homepage continuation for zzapi. The output starts immediately after the existing frozen Hero and redesigns only the content below it. The concept is a "Signal Corridor": one continuous vertical route spine carries a request through model identity, gateway policy, infrastructure and developer integration, creating a coherent technical story rather than separate marketing sections.
Input images:
- Image 1: exact current zzapi Hero and brand-language reference. Do not redraw or redesign the Hero; the generated canvas begins below it.
- Image 2: hierarchy reference for a large model identity object only. Do not copy its purple palette, certification claims, circular percentage, layout, typography, logo, copy, or components.
- Image 3: rhythm reference for a large data object only. Do not copy its purple/orange palette, chart, accordion, layout, typography, logo, copy, or components.

Target dimensions: a 1440px-wide scrollable landing page direction, rendered on a 2160x3840 portrait canvas. Show only the below-fold continuation; no browser chrome, no device frame, no duplicated navigation, no Hero reconstruction.

Audience and goal: developers and technical buyers evaluating an AI API gateway. Communicate that one request moves through understandable, observable stages.

Visual system: preserve zzapi's existing minimal premium technical language from Image 1. White base, near-black type, pale gray spatial bands, hairline silver rules, zzapi blue used only on the active signal and route spine. Editorial precision, quiet engineering, wide whitespace, strong but restrained typography. No purple, no dark-blue takeover, no broad gradients, no glass, no neon, no ornamental blobs.

Information hierarchy and layout:
- Begin with a quiet Hero-to-content transition and a small vertical signal emerging from the existing route direction.
- Use a continuous thin route spine that moves down the page and changes alignment at section boundaries. It is the only repeated visual motif.
- First major section is an offset 40/60 layout. A very large model ID and short factual labels sit beside an exploded identity stack: Catalog -> Vendor metadata -> Endpoint -> Recent traffic. The active step has a compact blue marker; other states are black/gray text.
- Next, the route spine becomes a large vertical sequence: Client, zzapi Gateway, Policy route, Model, Response. Each stage has one sentence and one truthful state. This must read like an engineering cutaway, not a timeline component in a card.
- Then the spine branches into a sparse infrastructure constellation with three current catalog models; use thin hairlines and plenty of blank space, not a network mesh.
- Developer experience closes the route with one code block aligned to the spine and a model-switch line showing that only the model ID changes.
- End with a confident CTA that visually reconnects to the route spine, then existing project attribution/footer language.

Text (verbatim):
- "From model identity to a real request"
- "CURRENT CATALOG"
- "gpt-5.6-terra"
- "Catalog metadata"
- "Configured"
- "Endpoint"
- "POST /v1/chat/completions"
- "Recent traffic"
- "Observed"
- "Client"
- "zzapi Gateway"
- "Policy route"
- "Model"
- "Response"
- "Current model catalog"
- "gpt-5.6-sol"
- "deepseek-v4-flash"
- "Change the model ID, keep the integration path"
- "Open Console"
- "Read Docs"

Constraints: render only truthful qualitative states; no official certification; no Verified; no Authentic Model; no 100%; no 99.99%; no latency, uptime, request-count, cost, or unsupported capability claims. Do not create a card wall, stepper widget, three-column feature grid, radial percentage, or bar chart. Use clear grouping, alignment and dividers before borders. Render the listed text directly, exactly once where used, with no invented marketing numbers, no third-party logos, no watermark.

Responsive intent: the route spine must naturally become a single left-aligned mobile reading path; desktop-only absolute-positioned nodes are not acceptable.
